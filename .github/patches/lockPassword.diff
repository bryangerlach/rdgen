diff --git a/flutter/lib/models/server_model.dart b/flutter/lib/models/server_model.dart
index 877576461..8a1b2c3f4 100644
--- a/flutter/lib/models/server_model.dart
+++ b/flutter/lib/models/server_model.dart
@@ -64,6 +64,13 @@ class ServerModel with ChangeNotifier {
   int get connectStatus => _connectStatus;
 
   String get verificationMethod {
+    // Check if verification method is locked via custom config
+    final lockedMethod = bind.mainGetBuildinOption(key: 'locked-verification-method');
+    if (lockedMethod == 'Y') {
+      // Force permanent password when locked - users cannot change to one-time password
+      return kUsePermanentPassword;
+    }
+
     final index = [
       kUseTemporaryPassword,
       kUsePermanentPassword,
@@ -71,6 +78,9 @@ class ServerModel with ChangeNotifier {
     ].indexOf(_verificationMethod);
     if (index < 0) {
       return kUseBothPasswords;
     }
     return _verificationMethod;
   }
